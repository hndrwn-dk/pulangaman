import 'package:flutter/material.dart';

/// Visual tokens for Phase 0 shell. Expand in Phase 1 screens.
abstract final class AppColors {
  static const teal = Color(0xFF0F6B5C);
  static const tealDeep = Color(0xFF0A4F44);
  static const sand = Color(0xFFE7F0EC);
  static const canvas = Color(0xFFF3F7F5);
  static const ink = Color(0xFF12201C);
  static const danger = Color(0xFFB42318);
}
