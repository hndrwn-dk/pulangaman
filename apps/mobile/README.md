# PulangAman Mobile

Flutter shell for Phase 0.

```bash
flutter pub get
flutter analyze
flutter test
flutter run
```

User-facing strings are Indonesian (`lib/core/strings.dart`). Code and comments stay in English.
